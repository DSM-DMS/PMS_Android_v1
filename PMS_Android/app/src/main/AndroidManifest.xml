<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pms_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".introduce_detail.Introduce_Club_Detail_Activity"></activity>
        <activity android:name=".Activity.IntroduceCompanyActivity" />
        <activity android:name=".Activity.IntroduceDeveloperActivity" />
        <activity android:name=".passwordActivity" />
        <activity android:name=".fragments.Firstmypage" />
        <activity android:name=".login.SigninActivity" />
        <activity android:name=".login.MainLoginActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>